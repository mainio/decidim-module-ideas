<figure>
  <ul class="tags tags--idea tags--list">
    <% if category.present? %>
      <li>
        <%= link_to translated_attribute(category.name), resource_locator(model).index(filter: { category_id: [category.id.to_s] }) %>
        <% if previous_category.present? && show_previous_category? %>
          &nbsp;
          <small class="text-small">
            <%= t("changed_from", scope: "decidim.ideas.ideas.tags", previous_name: "#{previous_category.translated_name}").html_safe %>
          </small>
        <% end %>
      </li>
    <% end %>

    <% if has_visible_area_scope?(model) %>
      <li>
        <%= link_to translated_attribute(area_scope.name), resource_locator(model).index(filter: { scope_id: [area_scope.id] }) %>
        <% if previous_area_scope.present? && show_previous_area_scope? %>
          &nbsp;
          <small class="text-small">
            <%= t("changed_from", scope: "decidim.ideas.ideas.tags", previous_name: "#{translated_attribute(previous_area_scope.name)}").html_safe %>
          </small>
        <% end %>
      </li>
    <% end %>
  </ul>
</figure>
