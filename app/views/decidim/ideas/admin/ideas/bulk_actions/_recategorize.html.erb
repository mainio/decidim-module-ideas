<div id="js-recategorize-ideas-actions" class="hidden js-bulk-action-form">
  <%= form_tag(update_category_ideas_path, method: :post, remote: true, id: "js-form-recategorize-ideas", class: "flex--cc flex-gap--1") do %>
    <div class="checkboxes hidden">
      <% ideas.each do |idea| %>
        <%= check_box_tag "idea_ids[]", idea.id, false, class: "js-check-all-idea js-idea-id-#{idea.id}" %>
      <% end %>
    </div>

    <%= bulk_categories_select current_component.categories %>

    <%= submit_tag(t("decidim.ideas.admin.ideas.index.update"), id: "js-submit-edit-category", class: "button small button--simple float-left") %>

    <button id="js-cancel-edit-category" class="button tiny clear compact js-cancel-bulk-action" type="button"><%= t("decidim.ideas.admin.ideas.index.cancel") %></button>
  <% end %>
</div>
